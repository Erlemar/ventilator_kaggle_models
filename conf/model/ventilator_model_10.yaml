class_name: src.models.ventilator_model_10.VentilatorNet
params:
  input_dim: 5
  lstm_dim: 512
  dense_dim: 512
  logit_dim: 512
  num_layers: 1
  n_classes: ${training.n_classes}
  initialize: True
  init_style: 1
  use_mlp: True
  simpler_mlp: False
  activation: torch.nn.PReLU
  activation2: torch.nn.SELU
  use_layer_norm: False
  layer_norm_style: 1
  layer_norm_logits: False
  lstm_dropout: 0.0
  dropout: 0.0
